# PairLingua Frontend

## Описание проекта

**PairLingua Frontend** — это современное веб-приложение для интерактивного изучения испанского языка с использованием метода Leitner (спaced repetition). Фронтенд разработан на React 19 с использованием Vite в качестве сборщика, обеспечивает быструю разработку и оптимизацию для продакшена. Приложение предоставляет пользователям удобный интерфейс для обучения, управления словарём и отслеживания прогресса.

## 🎯 Основные возможности

- **Аутентификация**: Регистрация и вход в систему
- **Интерактивное обучение**: Игровой режим с подбором пар слов
- **Личный словарь**: Добавление, редактирование и удаление словарных пар (испанский-русский)
- **Отслеживание прогресса**: Статистика по точности, серии правильных ответов и прогрессу
- **Адаптивный дизайн**: Работает на десктопах и мобильных устройствах
- **Toast уведомления**: Информирование пользователя об ошибках и успехах

## 📋 Стек технологий

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| **Фреймворк** | React | 19.1.1 |
| **Сборщик** | Vite | 7.1.2 |
| **Маршрутизация** | React Router DOM | 7.8.2 |
| **CSS-фреймворк** | MVP.css | 1.17.2 |
| **Уведомления** | React Toastify | 11.0.5 |
| **Контроль качества** | ESLint | 9.33.0 |
| **TypeScript конфиг** | TypeScript | (конфиг) |
| **Node.js** | Node.js | 20+ |
| **npm** | npm | 10+ |

## 🗂️ Структура проекта

```
frontend/
├── public/                        # Статические ресурсы
│   ├── favicon_32x32.png
│   └── vite.svg
├── src/
│   ├── api/                       # API интеграция
│   │   ├── get_pairApi.js         # Получение пар слов для игры
│   │   ├── loginApi.js            # Логика входа и refresh токенов
│   │   ├── registerApi.js         # Регистрация пользователя
│   │   └── wordApi.js             # CRUD операции со словарями
│   ├── assets/                    # SVG, логотипы и изображения
│   │   └── react.svg
│   ├── components/                # React компоненты
│   │   ├── DictionaryPage.jsx     # Страница управления словарём
│   │   ├── LoginForm.jsx          # Форма входа
│   │   ├── MainGamePage.jsx       # Основная игровая страница (Leitner)
│   │   └── RegistrationForm.jsx   # Форма регистрации
│   ├── utils/                     # Вспомогательные функции
│   │   └── errorUtils.js          # Обработка ошибок
│   ├── App.jsx                    # Главный компонент, роутинг
│   ├── App.css                    # Стили приложения
│   ├── index.css                  # Глобальные стили
│   ├── main.jsx                   # Точка входа в приложение
│   └── __init_.py                 # (техн. файл для совместимости)
├── .gitignore
├── Dockerfile                    # Docker конфиг для контейнеризации
├── eslint.config.js              # Конфиг ESLint для контроля качества кода
├── index.html                    # HTML шаблон
├── package.json                  # Зависимости и скрипты
├── package-lock.json             # Зафиксированные версии
├── tsconfig.json                 # TypeScript конфиг
├── vite.config.js                # Конфиг Vite
├── frontend-README.md            # Этот файл
└── _init_.py                     # (техн. файл)
```

## 🚀 Установка и запуск

### Требования

- **Node.js**: 20+ (рекомендуется LTS версия)
- **npm**: 10+ (идёт в комплекте с Node.js)
- **Backend API**: Должна быть запущена на `http://localhost:8000` (или настроить proxy)

### Локальная установка

#### 1. Клонирование репозитория и переход в директорию

```bash
git clone <repository-url>
cd frontend
```

#### 2. Установка зависимостей

```bash
npm install
```

#### 3. Запуск dev сервера

```bash
npm run dev
```

Приложение запустится на `http://localhost:5173`

**Автоматическая перезагрузка:** При изменении файлов dev сервер автоматически перезагружает приложение в браузере.

### Сборка для продакшена

```bash
npm run build
```

Результат сборки сохраняется в директории `dist/`. Эти файлы можно разместить на веб-сервере (Nginx, Apache и т.д.).

### Preview продакшн сборки локально

```bash
npm run preview
```

### Запуск через Docker

#### 1. Сборка Docker образа

```bash
docker build -t pairlingua-frontend .
```

#### 2. Запуск контейнера

```bash
docker run -p 5173:5173 pairlingua-frontend
```

Приложение станет доступно на `http://localhost:5173`

#### 3. С использованием docker-compose (рекомендуется)

```bash
 docker-compose up
```
## ⚙️ Конфигурация

### Vite Config (`vite.config.js`)

**Текущие настройки:**

```javascript
export default defineConfig({
  plugins: [react()],
  server: {
    host: '0.0.0.0',      // Доступно со всех интерфейсов
    port: 5173,            // Порт разработки
    proxy: {
      '/api': 'http://backend:8000'  // Проксирование API запросов
    },
  },
});
```

### TypeScript Config (`tsconfig.json`)

Конфиг поддерживает современный JavaScript (ESNext), JSX синтаксис и строгую типизацию.

### ESLint Config (`eslint.config.js`)

Применяются best-practices для React:
- `react-hooks/recommended`: Правила для React Hooks
- `react-refresh/vite`: Поддержка быстрой перезагрузки (HMR)

**Запуск проверки кода:**

```bash
npm run lint
```

## 🔐 Аутентификация

### Как работает логин/регистрация

1. **Регистрация** (`registerApi.js`):
   - Пользователь заполняет email и пароль
   - Отправляется POST запрос на `/api/v1/auth/register`
   - Backend возвращает информацию о пользователе

2. **Вход** (`loginApi.js`):
   - Пользователь вводит email и пароль
   - Отправляется POST запрос на `/api/v1/auth/login`
   - Backend возвращает access и refresh токены (в cookies)
   - Приложение перенаправляет пользователя на `/game`

3. **Refresh токены** (`fetchWithAuth`):
   - При получении статуса 401 (Unauthorized)
   - Автоматически вызывается `refreshTokens()`
   - Повторяется оригинальный запрос с новым токеном

### Хранение токенов

Токены хранятся в **httpOnly cookies** (автоматически отправляются с каждым запросом), что безопаснее хранения в localStorage.

**Пример запроса с аутентификацией:**

```javascript
const response = await fetch('/api/v1/users/me', {
  credentials: 'include',  // Отправляет cookies автоматически
});
```

## 📡 API Интеграция

### API Файлы

#### `loginApi.js`
```javascript
// Вход пользователя
onLogin({ email, password })

// Универсальный fetch с автоматическим refresh токенов
fetchWithAuth(url, options, retry)
```

#### `registerApi.js`
```javascript
// Регистрация нового пользователя
onRegister({ email, password })
```

#### `get_pairApi.js`
```javascript
// Получить случайные пары слов для игры (limit = 5 по умолчанию)
fetchWordPairs(limit)
```

#### `wordApi.js`
```javascript
// Получить все пары слов пользователя
getWordPairs()

// Добавить новую пару
addWordPair(wordData)  // { spanish_word, russian_word }

// Обновить пару
updateWordPair(id, wordData)

// Удалить пару
deleteWordPair(id)
```


## 🎮 Основные компоненты

### LoginForm.jsx

**Функционал:**
- Форма входа с email и паролем
- Валидация email
- Обработка ошибок
- Перенаправление на `/game` после успешного входа

### RegistrationForm.jsx

**Функционал:**
- Форма регистрации
- Проверка совпадения паролей
- Валидация email
- Перенаправление на `/login` после регистрации

### MainGamePage.jsx

**Это основной компонент игры (метод Leitner):**

```javascript
const [stats, setStats] = useState({
  score: 0,              // Количество правильных ответов
  best: 10,              // Максимальный счёт
  streak: 0,             // Текущая серия правильных ответов
  accuracy: 0,           // Точность в %
  progress: 0,           // Общее количество попыток
  isError: false,        // Была ли ошибка в последнем ответе
});
```

**Логика:**
1. Загружает 5 случайных пар слов через `fetchWordPairs()`
2. Отображает испанские слова слева, русские справа (перемешанные)
3. При клике на испанское и русское слово проверяет соответствие
4. Если верно — убирает пару, обновляет статистику
5. Если неверно — показывает ошибку, сбрасывает серию
6. Когда все пары кончились — загружает новый набор

### DictionaryPage.jsx

**Функционал:**
- Отображение всех добавленных пар слов в таблице
- Добавление новой пары через форму
- Редактирование выбранной пары
- Удаление пары с подтверждением
- Обработка состояний загрузки и ошибок

## 🎨 Стили и дизайн

### MVP.css

Приложение использует минималистичный CSS фреймворк **MVP.css**, который обеспечивает:
- Чистый и минималистичный дизайн
- Встроенные стили для HTML элементов (button, form, table и т.д.)
- Хороший вид без дополнительных стилей

**Подключение:**
```javascript
import 'mvp.css';
```

### Кастомные стили

- `index.css`: Глобальные стили приложения
- `App.css`: Стили для главного компонента


## 📞 React Toastify

Уведомления используют **react-toastify** для информирования пользователя:

```javascript
import { ToastContainer, toast } from 'react-toastify';

// Успешное сообщение
toast.success('Регистрация успешна!');

// Сообщение об ошибке
toast.error('Ошибка при входе');

// Информационное сообщение
toast.info('Загружаю данные...');
```

## 🔄 React Router DOM

**Маршруты приложения:**

| Маршрут | Компонент | Описание |
|---------|-----------|---------|
| `/login` | LoginForm | Страница входа |
| `/register` | RegistrationForm | Страница регистрации |
| `/game` | MainGamePage | Основной игровой интерфейс |
| `/dictionary` | DictionaryPage | Управление словарём |

**Пример роутинга в App.jsx:**

```javascript
<Routes>
  <Route path="/login" element={<LoginForm onLogin={handleLogin} />} />
  <Route path="/register" element={<RegistrationForm onRegister={handleRegister} />} />
  <Route path="/game" element={<MainGamePage />} />
  <Route path="/dictionary" element={<DictionaryPage />} />
</Routes>
```

## 🧪 Контроль качества кода

### ESLint

**Запуск проверки:**

```bash
npm run lint
```

**Проверяет:**
- Неиспользуемые переменные
- Нарушения правил React Hooks
- Некорректное использование refresh компонентов

### Best Practices

1. **Компоненты на функциях**: Все компоненты написаны как функциональные компоненты с Hooks
2. **useState/useEffect**: Правильное использование React Hooks
3. **Зависимости эффектов**: Все зависимости указаны в массивах зависимостей
4. **Обработка асинхронного кода**: Используется async/await с try-catch

## 📊 Управление состоянием

Приложение использует **встроенные React Hooks** для управления состоянием:

- `useState`: Локальное состояние компонентов
- `useEffect`: Побочные эффекты (загрузка данных, подписки)
- `useNavigate`: Навигация между маршрутами
- `useLocation`: Получение текущего маршрута

**Пример:**

```javascript
const [words, setWords] = useState([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState('');

useEffect(() => {
  fetchWords();
}, []); // Запускается один раз при монтировании
```

## 🐛 Решение проблем

### Проблема: "Cannot find module" ошибка

**Решение:**
```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Проблема: Proxy не работает (ошибка CORS)

**Проверь `vite.config.js`:**
```javascript
proxy: {
  '/api': 'http://backend:8000'  // Должен совпадать с адресом backend
}
```

**Для Docker:** Backend должен быть доступен по имени сервиса: `http://api:8000`

### Проблема: "Module not found: Can't resolve 'mvp.css'"

**Решение:**
```bash
npm install mvp.css
```

### Проблема: Токены не сохраняются

**Причина:** Возможна проблема с CORS и cookies.

**Решение:**
- Проверь, что backend отправляет `Set-Cookie` headers
- Проверь `credentials: 'include'` во всех fetch запросах
- Убедись, что `SameSite` cookie флаг настроен правильно на backend

### Проблема: Hot Module Replacement (HMR) не работает

**Решение:**
```bash
# Перезапусти dev сервер
npm run dev
```

Если всё ещё не работает, проверь, что Vite хост настроен на `0.0.0.0`:

```javascript
// vite.config.js
server: {
  host: '0.0.0.0',
}
```

## 🔗 Полезные ссылки

- [React документация](https://react.dev)
- [Vite документация](https://vitejs.dev)
- [React Router документация](https://reactrouter.com)
- [MVP.css документация](https://andybrewer.github.io/mvp/)
- [React Toastify документация](https://fkhadra.github.io/react-toastify/introduction)
- [ESLint документация](https://eslint.org)

## 👥 Контакты и поддержка
- **Frontend версия**: 0.0.1
- **Связь с backend**: API на `http://localhost:8000` (dev) или через env variables (prod)
- **Разработка**: Node.js 20+, npm 10+


**Последнее обновление**: Ноябрь 2025

**Статус**: В активной разработке ✅