# PairLingua Backend

## Описание проекта

**PairLingua** — это современный асинхронный REST API для интерактивного приложения изучения языков с использованием алгоритма спaced repetition. Бэкенд разработан на FastAPI и обеспечивает полный набор функций для управления пользователями, аутентификацией, учебными материалами и отслеживанием прогресса.

## 📋 Стек технологий

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| **Фреймворк** | FastAPI | 0.104.1 |
| **Web-сервер** | Uvicorn | 0.24.0 |
| **ORM & Миграции** | SQLAlchemy, Alembic | 2.0.23, 1.13.0 |
| **База данных** | PostgreSQL | - |
| **Кэш & Хранилище** | Redis | 5.0.1 |
| **Аутентификация** | JWT (python-jose) | 3.3.0 |
| **Хеширование** | bcrypt, passlib | 4.1.2, 1.7.4 |
| **Валидация** | Pydantic | 2.5.0 |
| **Управление зависимостями** | Poetry | - |
| **Контейнеризация** | Docker | - |
| **Мониторинг** | Prometheus client | 0.19.0 |
| **Python** | Python | >=3.12, <4.0 |

## 🗂️ Структура проекта

```
backend/
├── alembic/                    # Миграции базы данных
│   ├── versions/              # SQL-миграции
│   ├── env.py
│   └── script.py.mako
├── app/
│   ├── api/                   # API слой
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── router.py      # Главный роутер API v1(модуль маршрутизации)
│   │       ├── achievements.py    # Эндпоинты достижений
│   │       ├── auth.py            # Аутентификация и авторизация
│   │       ├── study.py           # Логика изучения
│   │       ├── users.py           # Управление пользователями
│   │       └── words.py           # Работа со словами
│   ├── core/                  # Ядро приложения
│   │   ├── __init__.py
│   │   ├── config.py          # Конфигурация (Pydantic Settings)
│   │   ├── database.py        # SQLAlchemy engine и sessionmaker
│   │   ├── redis.py           # Клиент Redis и RedisService
│   │   ├── exceptions.py      # Кастомные исключения
│   │   └── security.py        # JWT, хеширование паролей
│   ├── models/                # ORM модели SQLAlchemy
│   │   ├── __init__.py
│   │   ├── achievement.py
│   │   ├── review.py
│   │   ├── session.py
│   │   ├── user.py
│   │   ├── user_card.py
│   │   └── word_pair.py
│   ├── schemas/               # Pydantic схемы (для валидации и ответов)
│   │   ├── __init__.py
│   │   ├── auth.py
│   │   ├── study.py
│   │   ├── user.py
│   │   └── word.py
│   ├── services/              # Бизнес-логика (service layer)(пока в разработке)
│   │   ├── __init__.py
│   │   ├── auth_service.py    # Логика аутентификации
│   │   ├── sm2_service.py     # SM-2 алгоритм повторения
│   │   ├── study_service.py   # Логика обучения
│   │   ├── user_service.py    # Работа с пользователями
│   │   └── word_service.py    # Работа со словами
│   ├── utils/                 # Утилиты
│   │   ├── __init__.py
│   │   ├── email.py           # Отправка писем
│   │   ├── security.py        # Дополнительные функции безопасности
│   │   └── main.py            # Точка входа приложения
│   └── __init__.py
├── .env                       # Переменные окружения (в .gitignore)
├── .env.example              # Шаблон переменных окружения
├── alembic.ini               # Конфиг Alembic
├── Dockerfile                # Docker конфиг
├── poetry.lock               # Зафиксированные версии зависимостей
├── pyproject.toml            # Конфиг Poetry и зависимости
├── backend-README.md         # Этот файл
```

## 🚀 Установка и запуск

### Требования

- **Python**: 3.12 или выше
- **PostgreSQL**: 12+
- **Redis**: 6.0+
- **Poetry**: последняя версия

### Локальная установка

#### 1. Клонирование репозитория и переход в директорию

```bash
git clone <repository-url>
cd backend
```

#### 2. Установка зависимостей через Poetry

```bash
poetry install
```

#### 3. Подготовка файла конфигурации

Скопируйте шаблон `.env.example` в `.env` и отредактируйте значения:

```bash
cp .env.example .env
```

**Важные переменные в `.env`:**

```env
# Общие настройки
DEBUG=true
ENVIRONMENT=development

# База данных PostgreSQL
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/pairlingua

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT ключи (генерируйте безопасные значения)
JWT_SECRET_KEY=your-super-secret-key-here
JWT_REFRESH_SECRET_KEY=your-refresh-secret-key-here

# CORS (для фронтенда)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Email (опционально)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
```

#### 4. Применение миграций базы данных

```bash
poetry run alembic upgrade head
```

#### 5. Запуск сервера в режиме разработки

```bash
poetry run uvicorn app.utils.main:app --reload
```

Сервер запустится на `http://localhost:8000`

**Интерактивная документация API:**
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

### Запуск через Docker

#### 1. Сборка образа

```bash
docker build -t pairlingua-backend .
```

#### 2. Запуск контейнера

```bash
docker run --env-file .env -p 8000:8000 --name pairlingua-api pairlingua-backend
```

#### 3. С использованием docker-compose (рекомендуется для разработки)
В корне проекта есть готовый сконфигурированный файл docker-compose.yml, который запускает сразу все необходимые сервисы: PostgreSQL, Redis и твой FastAPI-бэкенд.

Особенности этого docker-compose.yml:
Сервис db — PostgreSQL с пользовательскими параметрами и постоянным томом данных.

Сервис redis — Redis сервер для кэширования и быстрых операций.

Сервис api — твой FastAPI, который строится из Dockerfile проекта, с командой запуска через poetry + uvicorn.

Все сервисы связаны через сеть Docker, backend ожидает готовности базы и Redis.

Проброс портов сделан для доступа к сервисам с локальной машины.

Чтобы поднять все сервисы вместе, достаточно в корне проекта выполнить:

```bash
 docker-compose up
```

## 🔐 Конфигурация и безопасность

### Переменные окружения

Все конфигурационные параметры находятся в классе `Settings` в файле `app/core/config.py`:

- **APP_NAME**: Название приложения (по умолчанию: "PairLingua API")
- **APP_VERSION**: Версия приложения
- **DEBUG**: Режим отладки (не использовать в production)
- **ENVIRONMENT**: Тип окружения (development, testing, production)
- **DATABASE_URL**: Строка подключения к PostgreSQL
- **REDIS_URL**: URL для подключения к Redis
- **JWT_SECRET_KEY**: Секретный ключ для создания access токенов
- **JWT_REFRESH_SECRET_KEY**: Секретный ключ для refresh токенов
- **JWT_ALGORITHM**: Алгоритм для подписания JWT (по умолчанию: HS256)
- **ACCESS_TOKEN_EXPIRE_MINUTES**: Время жизни access токена (по умолчанию: 15 минут)
- **REFRESH_TOKEN_EXPIRE_DAYS**: Время жизни refresh токена (по умолчанию: 7 дней)
- **CORS_ORIGINS**: Разрешённые источники CORS (может быть строка или JSON массив)
- **SMTP_***: Параметры для отправки писем (опционально)

### Аутентификация и авторизация

Приложение использует **JWT (JSON Web Tokens)** с двумя типами токенов:

1. **Access Token**: Короткоживущий токен (15 минут по умолчанию) для доступа к API
2. **Refresh Token**: Долгоживущий токен (7 дней по умолчанию) для получения новых access токенов

**Функции безопасности** (в `app/core/security.py`):

- `create_password_hash(password)`: Хеширование пароля с bcrypt
- `verify_password(plain_password, hashed_password)`: Проверка пароля
- `create_access_token(subject)`: Создание access токена
- `create_refresh_token(subject)`: Создание refresh токена
- `decode_token(token, token_type)`: Декодирование и валидация токена


## 📊 База данных и миграции

### SQLAlchemy ORM

Проект использует SQLAlchemy 2.0 с асинхронной поддержкой. Модели находятся в `app/models/`:

- **User**: Пользователь приложения
- **WordPair**: Пара слов для изучения
- **UserCard**: Карточка пользователя (прогресс изучения)
- **Session**: Сессия обучения
- **Review**: Отзыв о пройденном материале
- **Achievement**: Достижения пользователя

### Alembic миграции

**Создание новой миграции:**

```bash
poetry run alembic revision --autogenerate -m "Описание изменения"
```

**Применение миграций:**

```bash
poetry run alembic upgrade head
```

**Откат на одну миграцию назад:**

```bash
poetry run alembic downgrade -1
```

**История миграций:**

```bash
poetry run alembic history
```

Все миграции хранятся в директории `alembic/versions/` в виде Python файлов.

## 💾 Redis и кэширование

### RedisService

Класс `RedisService` в `app/core/redis.py` предоставляет простой интерфейс для работы с Redis:

```python
from app.core.redis import redis_service

# Получить значение
value = await redis_service.get("key")

# Установить значение с TTL (время жизни в секундах)
await redis_service.set("key", "value", ex=3600)

# Удалить ключ
await redis_service.delete("key")

# Проверить наличие ключа
exists = await redis_service.exists("key")

# Установить время жизни для ключа
await redis_service.expire("key", 3600)

# Добавить элемент в set
await redis_service.sadd("set_key", "element1", "element2")

# Проверить наличие элемента в set
is_member = await redis_service.sismember("set_key", "element1")
```

**Используется для:**
- Кэширования часто запрашиваемых данных
- Хранения сессий
- Чёрного списка (blacklist) инвалидированных токенов
- Временного хранения OTP и кодов подтверждения

## 📡 API эндпоинты

### Основные категории эндпоинтов

#### Аутентификация (`/api/v1/auth`)
- `POST /register` — Регистрация нового пользователя
- `POST /login` — Вход в систему
- `POST /refresh` — Получение нового access токена
- `POST /logout` — Выход из системы
- `POST /forgot-password` — Запрос на сброс пароля
- `POST /logout` — Сброс пароля

#### Пользователи (`/api/v1/users`)
- `GET /me` — Получить информацию о текущем пользователе
- `PATCH /me` — Обновить профиль пользователя
- `GET /me/stats` — Получить статистику обучения текущего пользователя
- `GET /me/stats/detailed` — Получите подробную статистику обучения с указанием диапазона дат
- `GET /me/change-password` — Изменить пароль пользователя
- `DELETE /me` — Удалить аккаунт

#### Слова и материалы (`/api/v1/pairs`)

- `GET /pairs` — Поиск слов с фильтрами и прогрессом пользователя
- `GET /pairs/random_simple` — Получить случайные слова без фильтров
- `GET /pairs/{word_pair_id}` — Получить слово по ID
- `POST /pairs` — Создать новое слово (только для админов)
- `POST /pairs/batch` — Создать несколько слов одновременно (только для админов)
- `PATCH /pairs/{word_pair_id}` — Обновить слово (только для админов)
- `DELETE /pairs/{word_pair_id}` — Мягко удалить слово (только для админов)


#### Обучение (`/api/v1/study`)

- `GET /cards/due` — Получить карточки, нуждающиеся в повторении с использованием алгоритма интервального повторения
- `POST /cards/review` — Отправить результаты повторения и обновить интервалы по алгоритму SM-2
- `POST /session/replace` — Заменить изученную карточку новой в текущей сессии
- `GET /session/{session_id}/stats` — Получить статистику по конкретной сессии обучения
- `GET /leaderboard/weekly` — Получить еженедельный рейтинг лидеров
- `GET /progress/overview` — Получить обзор прогресса обучения и предстоящих повторений

#### Достижения (`/api/v1/achievements`)
- `GET /available` — Получить список доступных достижений
- `GET /available/me` — Получить достижения, заработанные текущим пользователем
- `GET /available/progress` — Получить прогресс по незавершённым достижениям


#### Здоровье приложения
- `GET /api/v1/health` — Проверить статус API и зависимостей
- `GET /api/v1/metrics` — Метрики Prometheus

## 📈 Логирование и мониторинг

### Логирование

Приложение использует встроенный модуль `logging` с форматом:

```
%(asctime)s - %(name)s - %(levelname)s - %(message)s
```

Логи записываются в консоль и дополнительно могут быть направлены в файл или систему мониторинга.

**Уровни логирования:**
- `INFO`: Информационные сообщения о работе приложения
- `DEBUG`: Детальная информация для отладки
- `WARNING`: Предупреждения
- `ERROR`: Ошибки при выполнении

**Пример:**

```python
import logging

logger = logging.getLogger(__name__)

logger.info("Пользователь вошёл в систему")
logger.error("Ошибка подключения к БД", exc_info=True)
```

### Мониторинг

- **Prometheus метрики**: `/api/v1/metrics`
- **Health check**: `/api/v1/health` (проверяет состояние БД и Redis)

## 🔄 Жизненный цикл приложения

### Стартап

При запуске приложения (`main.py`):

1. Инициализация логирования
2. Создание таблиц в БД (если их нет)
3. Подключение к PostgreSQL
4. Подключение к Redis
5. Регистрация middleware (CORS, TrustedHost, логирование)
6. Регистрация обработчиков исключений
7. Загрузка маршрутов API

### Middleware

- **CORS**: Для работы с фронтенд-приложением
- **TrustedHost**: Защита от атак
- **Request Logging**: Логирование всех HTTP запросов с временем выполнения

## 🚨 Обработка ошибок

Приложение имеет кастомные исключения в `app/core/exceptions.py`:

```python
from fastapi import HTTPException, status

# Общее исключение приложения
class PairLinguaException(HTTPException):
    def __init__(self, message: str, status_code: int = 400):
        self.message = message
        self.status_code = status_code

# Использование
raise PairLinguaException("User not found", status_code=404)
```

**Все ошибки возвращаются в формате:**

```json
{
  "error": {
    "code": "ExceptionClassName",
    "message": "Human readable message",
    "details": "Additional info (только в DEBUG режиме)",
    "trace_id": "unique-trace-id"
  }
}
```

## 📝 Ambiente (окружение для разработки)

### Рекомендуемые инструменты

- **IDE**: PyCharm или VS Code с расширением Python
- **Версионирование**: Git
- **Docker**: Для контейнеризации

### Полезные команды

```bash
# Проверка кода
poetry run pylint app/

# Форматирование кода
poetry run black app/

# Проверка типов
poetry run mypy app/

# Linting
poetry run flake8 app/

# Создание миграции с автоопределением
poetry run alembic revision --autogenerate -m "Description"

# Просмотр текущего статуса БД
poetry run alembic current

# История всех миграций
poetry run alembic history --verbose
```

## 🚀 Deployment

### Production требования

1. **Секретные ключи**: Используйте сильные, случайные значения для JWT ключей
2. **DEBUG=False**: Всегда отключайте режим отладки
3. **HTTPS**: Используйте HTTPS для всех соединений
4. **Переменные окружения**: Не коммитьте `.env` в репозиторий
5. **Логирование**: Настройте централизованное логирование
6. **Мониторинг**: Используйте Prometheus + Grafana для мониторинга

### Пример production .env

```env
DEBUG=false
ENVIRONMENT=production
DATABASE_URL=postgresql+psycopg2://user:strong_password@db-host:5432/pairlingua
REDIS_URL=redis://:strong_password@redis-host:6379/0
JWT_SECRET_KEY=super-secret-random-key-min-32-chars
JWT_REFRESH_SECRET_KEY=another-super-secret-key-min-32-chars
CORS_ORIGINS=https://pairlingua.com,https://www.pairlingua.com
```

## 🐛 Решение проблем

### Проблема: "Connection refused" для PostgreSQL

**Решение:**
```bash
# Проверьте, что PostgreSQL запущен
sudo service postgresql status

# Проверьте DATABASE_URL в .env
# Формат: postgresql+psycopg2://user:password@host:port/dbname
```

### Проблема: "Redis connection error"

**Решение:**
```bash
# Проверьте, что Redis запущен
redis-cli ping

# Проверьте REDIS_URL в .env
# Формат: redis://[:password@]host:port/db_number
```

### Проблема: Миграции не применяются

**Решение:**
```bash
# Проверьте текущий статус
poetry run alembic current

# Попробуйте переприменить миграции
poetry run alembic downgrade base
poetry run alembic upgrade head
```

## 📚 Документация и ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [Alembic документация](https://alembic.sqlalchemy.org/)
- [Redis документация](https://redis.io/documentation)
- [JWT введение](https://jwt.io/introduction)
- [Pydantic документация](https://docs.pydantic.dev/)